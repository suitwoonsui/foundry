export function SiteFooter() {
  return (
    <footer className="mt-20 border-t border-white/10 bg-black/20">
      <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6">
        <p className="text-sm text-zinc-500">
          Phase 1 hub — project visibility and mock funding bars. Wallet balances and live explorer links ship when the
          platform wiring is ready.
        </p>
        <p className="mt-3 text-xs text-zinc-600">
          The Community Foundry · Transparent per-project wallets · Participation paths, not pay-to-rank.
        </p>
      </div>
    </footer>
  );
}
